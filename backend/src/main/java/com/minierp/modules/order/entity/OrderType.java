package com.minierp.modules.order.entity;

public enum OrderType {
    SALES_ORDER,    // Satış Siparişi (Müşteri siparişi - onayda stok rezerve edilir)
    PURCHASE_ORDER  // Satın Alma Siparişi (Tedarikçi siparişi)
}

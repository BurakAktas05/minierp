package com.minierp.modules.partner.entity;

public enum PartnerType {
    CUSTOMER,   // Müşteri (Satış yapılan cari)
    SUPPLIER,   // Tedarikçi (Alış yapılan cari)
    BOTH        // Hem Müşteri Hem Tedarikçi
}
